import React from "react";

function ProgressBar() {

    return (
        <section className="bar-wrapper">
            <span className="bar-label">Period name</span>
            <div className="bar"></div>
        </section>
    );
}

export default ProgressBar;